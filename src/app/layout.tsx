import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dynamic Video Engine - AI-Powered Video Generation",
  description: "Scalable Dynamic Video Engine with AI routing, physics-based animations, and Remotion rendering. Built with TypeScript, Zod, and z-ai-web-dev-sdk.",
  keywords: ["Video Engine", "AI Video", "Remotion", "TypeScript", "Dynamic Video", "AI Routing", "React"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Dynamic Video Engine",
    description: "AI-Powered Video Generation with Physics-Based Animations",
    url: "https://chat.z.ai",
    siteName: "Z.ai",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Dynamic Video Engine",
    description: "AI-Powered Video Generation with Physics-Based Animations",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
